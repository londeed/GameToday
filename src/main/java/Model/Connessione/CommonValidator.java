package Model.Connessione;

import javax.servlet.http.HttpServletRequest;

public class CommonValidator
{
    /*public static Model.Connessione.RequestValidator validatePage(HttpServletRequest request)
    {
        Model.Connessione.RequestValidator validator=new Model.Connessione.RequestValidator(request);
        validator.assertInt("page", "Il numero di pagina deve essere in un formato valido");
        return validator;
    }

    public static Model.Connessione.RequestValidator validateId(HttpServletRequest request)
    {
        Model.Connessione.RequestValidator validator=new Model.Connessione.RequestValidator(request);
        validator.assertInt("id", "Id deve essere in un formato valido");
        return validator;
    }*/
}
